# x2y AV Ultimate v8.0.5 - Requirements
# Install: pip install -r requirements.txt

# ── Core UI (required) ───────────────────────────────────────────────────────
customtkinter>=5.2.0        # Modern dark widgets

# ── System monitoring (required) ─────────────────────────────────────────────
psutil>=5.9.0               # Process, network, memory monitoring

# ── Optional but strongly recommended ────────────────────────────────────────
yara-python>=4.3.0          # YARA rule scanning (compile from rules/*.yar)
pefile>=2023.2.7            # PE binary analysis for behavior scoring
watchdog>=3.0.0             # Real-time file system monitoring (Background Shield)
scapy>=2.5.0                # Packet capture (Traffic Capture feature)
wmi; sys_platform=="win32"  # WMI startup query (Windows only)

# ── Standard library (no install needed) ─────────────────────────────────────
# tkinter, hashlib, struct, re, threading, json, sqlite3, subprocess
# os, sys, time, shutil, math, socket, signal, csv, uuid, logging